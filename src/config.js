const SIZE = 500;
const CELL_SIZE = 10;
const ROWS = SIZE / CELL_SIZE;
const COLS = SIZE / CELL_SIZE;

const BG_COLOR = "#242424";
const COLOR_LIVE = "#ffffff";
const DEAD_COLOR = BG_COLOR;

export { SIZE, CELL_SIZE, ROWS, COLS, BG_COLOR, COLOR_LIVE, DEAD_COLOR };
